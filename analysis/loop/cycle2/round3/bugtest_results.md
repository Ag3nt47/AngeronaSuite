# Cycle 2 / Round 3 - Final Bug Test / QA Results

Date: 2026-07-14. Environment: supported Windows virtual environment with
`PYTHONPATH=src`; Qt ran offscreen. Scope: independent final verification of
the combined Cycle 2 tree after C2-R3-01 token-collision remediation. All prior
Cycle 2 reports were reviewed before testing. No visible GUI, host process,
rule, runtime configuration, or unrelated concurrent edit was changed.

## Compile and tree-integrity gates

- Complete source/tools/tests/root-runner compile: **205/205 Python files
  passed**, 0 failed.
- Production-package compile helper: **195/195 passed**, 0 failed.
- Conflict markers across source, tools, and tests: **0**.
- `git diff --check`: PASS; output contained only Windows line-ending notices.

## Module import, construction, and discovery

- `angerona.modules.*`: **62/62 module files imported**, 0 failures.
- `BaseModule` implementations: **61/61 constructed**, 0 constructor errors.
- `ModuleManager.discover()`: **61 modules discovered**, 0 discovery errors.
- Duplicate module names: **0**. Duplicate non-empty module codes: **0**.

## Complete focused regression set

Every direct repository test passed: **24/24**, 0 failed.

- Round 1 AAR correlation and severity-aware retention: **3/3**.
- Trusted-process policy, run-scoped drill resolution, runtime FIM target, and
  token-bound process correlation: **4/4**.
- Round 2 drill cancellation, immutable ARIA binding, pending-action TTL,
  confirmed research egress, and shutdown ownership: **5/5**.
- Response Safety Kernel shadow-only behavior: **7/7**.
- Round 3 forced confirmation-token collision: **1/1**.
- First-cycle lifecycle, sequential Eco wake-up, D-drive default, and scoped
  Ollama unload: **4/4**.

ARIA and research passed **12/12** standalone self-tests. The independent
Response Safety Kernel self-test also passed. No policy decision was promoted
from shadow data into confirmation, SOAR, or another host-action branch.

## Pending-action collision, concurrency, and TTL probe

A bounded in-memory probe independently exercised the new token lock and
retirement behavior:

- Forced UUID collision: **2 distinct staged actions retained and 2/2 executed
  only their bound callbacks/arguments**.
- Concurrent staging: **480/480 successful**, all 480 tokens unique.
- Concurrent double-confirm race: **480 legitimate confirmations executed
  exactly once**, 480 duplicate confirmations were refused, and 0 actions
  remained pending.
- TTL retirement: **200 expired snapshots removed**, leaving only 1 new valid
  action.

The probe found no overwrite, duplicate execution, deadlock, lost valid action,
or pending-state leak.

## Full application self-check

The final offscreen `tools/selfcheck.py` run passed **26/26 phases**, 0 failed.

- Discovered all **61 modules** and constructed the complete dashboard/dialog
  surface without showing a window.
- Raw module drill: **47 pass, 15 expected safe-harness skips, 0 genuine
  failures**. The skips are stopped/idle live sensors and optional Ollama.
- The benign live Red Team drill completed **30 steps over 2 phases** and
  cleaned its inert marker.
- Judgment/tamper gates, vetted remediation, persistence classification,
  coverage, incident correlation, world view, YARA/EICAR, WAL/retention, and
  audit-cache checks all passed.

## D-drive storage and diagnostics

Canonical path assertions passed:

- Data: `D:\local-security-ai\AngeronaSuite\runtime-data`
- `TEMP`, `TMP`, and `tempfile.gettempdir()`:
  `D:\local-security-ai\AngeronaSuite\runtime-data\tmp`

An initial ad-hoc assertion used an over-escaped comparison literal and failed
despite printing the correct D paths. The corrected canonical `Path` containment
assertion passed; this was a probe typo, not an Angerona defect.

Diagnostic review found no new genuine issue:

- `diagnostics/selftest_failures.json`: generated by this run with **0
  failures**.
- `diagnostics/crash.log`: unchanged since 15:58:55; its tail contains startup
  markers and historical thread snapshots, not a new crash.
- `diagnostics/not_responding.log`: unchanged since 19:36:56; this final run
  produced no watchdog record. The last entry is the previously classified
  headless self-check artifact.
- Rotated watchdog evidence remains bounded at about 4 MiB.

## Final QA decision

**Converged.** C2-R3-01 is closed and every combined-tree compile, module,
focused, ARIA/research, action-policy, concurrency/TTL, full-application,
D-drive, and diagnostic gate passed. **New production bugs: 0. Code changes in
this QA phase: 0.**
