"""macOS Observe platform support.

The Python package is intentionally observation-only.  Enforcement belongs in
signed, entitled native system extensions and is not represented as active
protection until those components are installed and healthy.
"""
