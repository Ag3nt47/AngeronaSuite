# ── GUI ──────────────────────────────────────────────────────────────────────
PySide6>=6.6

# ── Cross-platform system telemetry ─────────────────────────────────────────
psutil>=5.9
pywin32>=306 ; sys_platform == "win32"
WMI>=1.5 ; sys_platform == "win32"
scapy>=2.5 ; sys_platform == "win32"     # packet sniffer (also needs Npcap installed)
pywintrace>=0.2 ; sys_platform == "win32"  # ETW real-time process sensor (ETWR); needs elevation
yara-x>=1.15 ; sys_platform == "win32"       # in-process rules engine; stable ABI wheel
yara-x>=1.15 ; sys_platform == "linux" or sys_platform == "darwin"
PyJWT[crypto]>=2.8 ; sys_platform == "win32" # authenticated Microsoft Teams connector
cryptography>=50.0.0                         # AES-GCM/HKDF; includes 2026 oracle fix

# ── AI triage ────────────────────────────────────────────────────────────────
requests>=2.31

# ── Optional cloud escalation (only used if you supply keys) ──────────────────
# google-genai
# groq
# openai

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv>=1.0
PyYAML>=6.0.2                       # bounded local Sigma YAML import
defusedxml>=0.7.1                   # hardened Windows Event Log XML parsing

# Linux credentials use the OS Secret Service via the external `secret-tool`
# command from libsecret-tools. Angerona fails closed when it is unavailable.

# ── Local voice conversation (installed by the one-click installer) ──────────
sounddevice>=0.5
pyttsx3>=2.90
tqdm>=4.68
websockets>=16
